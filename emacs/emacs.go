// Package emacs holds the Emacs integration for dfd: ob-dfd.el, which
// renders dfd source blocks from org-mode. It carries no Go code; the
// test in this directory runs the elisp suite so that "go test ./..."
// covers the integration too.
package emacs
